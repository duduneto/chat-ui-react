import { ChatContainer, Container, ContentContainer, ChatContent, TypingContainer } from "./styles";

function Main() {
    return (
        <Container>
            <ContentContainer>

                <ChatContainer>
                    <ChatContent>

                    </ChatContent>
                    <TypingContainer>

                    </TypingContainer>
                </ChatContainer>
            </ContentContainer>
        </Container>
    );
}

export default Main;
